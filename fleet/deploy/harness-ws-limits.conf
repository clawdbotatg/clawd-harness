# Include only in h.atg.link's exact /ws location.
# 32 simultaneous links allows the whole household + nine workers to share an IP.
limit_conn harness_connections 32;
limit_conn_status 429;
limit_req zone=harness_handshakes burst=30 nodelay;
limit_req_status 429;
