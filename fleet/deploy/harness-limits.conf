# nginx http context: /etc/nginx/conf.d/harness-limits.conf
# These zones do nothing until the harness location opts in.
limit_conn_zone $binary_remote_addr zone=harness_connections:1m;
limit_req_zone $binary_remote_addr zone=harness_handshakes:1m rate=5r/s;
